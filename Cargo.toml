[package]
name = "reqwest-timeouts"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
reqwest = { version = "0.12" }
tokio = { version = "1", features = ["full"] }
